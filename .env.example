# Database (auto-provisioned by Replit — do not set manually on Replit)
DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# Email notifications via Resend (https://resend.com — free tier available)
# Create an account, verify a domain, and paste your API key here
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Admin dashboard password — change this to something strong
ADMIN_PASSWORD=change_me_to_something_strong
